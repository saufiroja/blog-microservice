package middlewares

// func LoggingMiddlewares(ctx context.Context, req interface{}, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (context.Context, error) {
// 	logger := slog.New(slog.NewTextHandler(os.Stderr, nil))

// }
